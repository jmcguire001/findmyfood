@model IEnumerable<SearchResultViewModel>

<h2>Search Results</h2>

@if (!Model.Any())
{
    <p>No results found for your search.</p>
}
else
{
    <div class="container mt-4">
        <div class="row">
            @foreach (var result in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card" style="background-color: #f8d5d5">
                        <!-- Header with Item Name -->
                        <div class="card-header text-white text-center" style="background-color: #d32f2f">
                            <h5 class="mb-0">@result.ItemName</h5>
                        </div>
                        <!-- Image Section -->
                        <div class="card-body text-center" style="background-color: #f8d5d5">
                            <img src="@result.ImageUrl" alt="@result.ItemName" class="img-fluid" style="max-height: 150px; object-fit: contain;" />
                        </div>
                        <!-- Details Section -->
                        <div class="card-body">
                            <p class="bg-secondary text-white px-2 py-1 rounded">
                                <i class="fas fa-tags"></i> @result.Category
                            </p>
                            <p class="bg-secondary text-white px-2 py-1 rounded">
                                <i class="fas fa-money-bill-wave"></i> $@result.Price
                            </p>
                            <p class="bg-secondary text-white px-2 py-1 rounded">
                                <i class="fas fa-store"></i> @result.StoreName
                            </p>
                            <p class="bg-secondary text-white px-2 py-1 rounded">
                                <i class="fas fa-map-marker-alt"></i> @result.City
                            </p>
                            <hr style="border: 1px solid black;" />
                            <p>@result.Description</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
